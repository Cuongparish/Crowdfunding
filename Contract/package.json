{
  "name": "crowdfunding-contract",
  "author": "team3",
  "version": "1.0.0",
  "scripts": {
    "build": "npx thirdweb@5.31.0 detect",
    "deploy": "npm run clean-build && npx thirdweb@5.31.0 deploy",
    "release": "npx thirdweb@5.31.0 release",
    "local-chain": "npx hardhat node --network hardhat",
    "clean-build": "rm -rf ./cache && rm -rf ./artifacts"
  },
  "devDependencies": {
    "hardhat": "^2.13.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "@thirdweb-dev/contracts": "^3.8.0",
    "dotenv": "^16.3.1"
  }
}
